# ============================================================
# Cloudflare Pages 重定向规则
#
# 本站为单页应用但**没有客户端路由**（未使用 react-router 等），
# 因此刻意不设置 `/* /index.html 200` 这类全站回退：
#   1) 全站回退会让任意不存在的路径都返回 200，形成「软 404」，
#      搜索引擎会判定为低质量收录，直接损害 SEO；
#   2) 保留真实 404 还能让已安装的旧 Service Worker 在浏览器
#      更新检查时拿到 404，从而按规范自动注销（撤不干净的问题）。
#
# 如需新增真实页面，请在此逐条书写精确规则，不要使用通配回退。
# ============================================================
