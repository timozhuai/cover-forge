# ============================================================
# Cloudflare Pages 重定向 / SPA 回退
# 静态资源（/assets/*、/*.png 等）优先于本规则匹配，
# 其余未命中的路径统一回退到 index.html，避免 404。
# ============================================================

/*    /index.html   200
